/**
* Commodity
*
* @interface Commodity
* @param name   {string}
* @param minPrice  {number}
* @param maxPrice {number}
*/
export interface Commodity {
    name: string;
    minPrice: number;
    maxPrice: number;
}
